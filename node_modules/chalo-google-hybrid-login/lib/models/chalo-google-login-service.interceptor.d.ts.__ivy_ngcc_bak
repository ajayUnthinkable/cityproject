import { HttpInterceptor, HttpEvent, HttpHandler, HttpRequest } from "@angular/common/http";
import { InjectionToken } from "@angular/core";
import { Observable } from "rxjs";
import { CookieService } from "ngx-cookie-service";
import { GoogleLoginService } from "../chalo-google-hybrid-login.service";
import { GoogleLoginConfig } from "./chalo-google-login.config";
export declare const GOOGLE_LOGIN_HTTP_INTERCEPTOR: InjectionToken<HttpInterceptor>;
export declare class GoogleAuthHttpInterceptor implements HttpInterceptor {
    private googleLoginInterceptor;
    private cookieService;
    private googleLoginService;
    private config;
    constructor(googleLoginInterceptor: HttpInterceptor, cookieService: CookieService, googleLoginService: GoogleLoginService<any>, config: GoogleLoginConfig);
    intercept(req: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>>;
}
