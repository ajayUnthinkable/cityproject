import { InjectionToken } from '@angular/core';
export interface GoogleLoginConfig {
    authCodeUrl: string;
    userTokenServerUrl: string;
    sendOtpUrl?: string;
    validateOtpUrl?: string;
    validateUserUrl?: string;
    auth: {
        google: {
            client_id: string;
            redirect_uri?: string;
            scope?: string;
        };
        server: {
            client_id: string;
            client_secret: string;
        };
    };
    user: {
        userCookie: string;
        authenticateUserUrl: string;
        loginStateCookie: string;
    };
    logout: {
        logoutUrl: string;
    };
}
export declare const GOOGLE_LOGIN_CONFIG: InjectionToken<GoogleLoginConfig>;
